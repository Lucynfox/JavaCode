package test;

public class StringManipulationExercise {
	public static void main(String[] args) {
		String str1 = "yesterday it was raining";
		String str2 = "today it is sunny";
		System.out.println(str2.toUpperCase() + ", " + str1.toUpperCase());
		String start = str2.toUpperCase().substring(0, 12);
		String end = str1.toUpperCase().substring(17, 24);
		System.out.println(start + end);
		
		wordCount("hello there my name is Lucy");
		boolean output = (word.indexOf(" ")
		System.out.println((word.indexOf(" "));
		
	}

	private static void wordCount(String word) {
		// TODO Auto-generated method stub
		int wordCount = 0;
		for (int i=0; i<word.length(); i++) {
			if (word.[i]) == " ") {
				wordCount += 1;
			}
			Char letter = word.charAt(i);
			if(letter.equals(" ")){
				wordCount += 1;
			}
		}
		int i = 0;
		while(i <word.length()) {
			if(word.indexOf(" ") != -1) {
				wordCount +=1;
				word.replace(, 0);
			};
				
			}
		}
	}
